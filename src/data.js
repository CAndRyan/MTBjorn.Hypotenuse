import {
    initializeDataStore,
    setState,
    getState,
    addStateListener,
    getStateAndListenForChanges
} from './data/dataStore';

export {
    initializeDataStore,
    setState,
    getState,
    addStateListener,
    getStateAndListenForChanges
};
